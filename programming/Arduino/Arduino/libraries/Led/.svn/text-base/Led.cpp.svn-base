#include <SPI.h>

#include "Led.h"

void Led::turnOn()
{
  this->setState(HIGH);
}
void Led::turnOff()
{
  this->setState(LOW);
}
/*char* Led::getLogMsg()
{
	String ergString = String("Led on Pin ") + this->pin + String(" is ");
  if(this->state == HIGH)
  	{
  		ergString += String("high");
  		char erg[256];
      	ergString.toCharArray(erg,256);
  		return erg;
	}
  else
  {
  		ergString += String("low");
  		char erg[256];
      	ergString.toCharArray(erg,256);
  		return erg;
  }
}*/